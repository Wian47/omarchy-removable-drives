{
  "schemaVersion": 1,
  "id": "wian47.removable-drives",
  "name": "Removable Drives",
  "version": "1.4.0",
  "author": "Wian Schoeman",
  "license": "MIT",
  "homepage": "https://github.com/Wian47/omarchy-removable-drives",
  "description": "USB sticks, SD cards, phones, and external drives in the Omarchy bar: mount, open, and safely eject.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Removable Drives",
    "description": "Appears only when something removable is attached. Mount, open, rename, check, and power off drives and phones without a terminal.",
    "category": "Files",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "alwaysShow": false,
      "openOnMount": true,
      "notifications": true,
      "fileManager": "",
      "refreshIntervalSec": 8,
      "barLabel": "none"
    },
    "schema": [
      {
        "key": "notifications",
        "type": "boolean",
        "label": "Notify on connect and remove",
        "defaultValue": true,
        "description": "Announce drives as they are plugged in, and warn when one is unplugged while still mounted."
      },
      {
        "key": "barLabel",
        "type": "enum",
        "label": "Text beside the icon",
        "options": [
          "none",
          "free",
          "name",
          "count"
        ],
        "defaultValue": "none",
        "description": "Show free space, the drive's name, or how many are attached. Vertical bars stay icon-only."
      },
      {
        "key": "alwaysShow",
        "type": "boolean",
        "label": "Always show the icon",
        "defaultValue": false,
        "description": "Keep the icon in the bar even when no removable drive is attached."
      },
      {
        "key": "openOnMount",
        "type": "boolean",
        "label": "Open after mounting",
        "defaultValue": true,
        "description": "Open the file manager once a volume finishes mounting."
      },
      {
        "key": "fileManager",
        "type": "string",
        "label": "File manager command",
        "defaultValue": "",
        "description": "Command used to open a mount point. Leave empty to use xdg-open."
      },
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Free-space refresh (seconds)",
        "min": 2,
        "max": 300,
        "step": 1,
        "defaultValue": 8,
        "description": "How often free space is re-read while the panel is open. Drives are detected instantly through udev either way."
      }
    ]
  }
}
